<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for ALL network access. Flutter auto-injects this into the
         debug/profile manifests only, so a RELEASE build (which merges just this
         `main` manifest) has NO network without it — every request fails and the
         app reports "can't reach the server". Caught on the first release build
         run on a real device (vc7 device-verify). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- A/V calls (LiveKit / WebRTC, #2726). CAMERA + RECORD_AUDIO gate
         getUserMedia; the rest are the flutter_webrtc-recommended set for audio
         routing (incl. Bluetooth headsets). Camera/mic are declared not-required
         so the install isn't gated for users who never call. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- The incoming-call ring (#3615, design 12 Decision 8). THIS LINE IS NOT
         THE GRANT, and on the devices that matter it is not even close.

         Below API 34 it is an ordinary permission and declaring it is enough.
         From API 34 it became a SPECIAL ACCESS permission, auto-granted only to
         apps Google has approved as calling or alarm apps — so on a modern
         handset this declaration buys the right to ASK, and nothing else.

         IT IS STILL A PRECONDITION FOR EVERYTHING DOWNSTREAM, which is the only
         reason it lands ahead of the approval. The Play Console declaration that
         seeks the grant does not appear until a bundle declaring this permission
         has been uploaded — so the form cannot be filled before this line exists,
         and #3615's "Nick opens the Console" was blocked on a one-line code
         change nobody had made. See IncomingCallNotifier.canRingFullScreen for
         the runtime half: the grant can be ABSENT with everything here correct,
         and the failure is silent — the notification posts as a heads-up banner
         and simply never takes over the screen. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <application
        android:label="Aiko Chat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
