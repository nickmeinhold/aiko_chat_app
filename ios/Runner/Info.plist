<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
	<dict>
		<key>CADisableMinimumFrameDurationOnPhone</key>
		<true/>
		<key>NSCameraUsageDescription</key>
		<string>Aiko Chat uses your camera for video calls.</string>
		<key>NSMicrophoneUsageDescription</key>
		<string>Aiko Chat uses your microphone for voice and video calls.</string>
		<key>CFBundleDevelopmentRegion</key>
		<string>$(DEVELOPMENT_LANGUAGE)</string>
		<key>CFBundleDisplayName</key>
		<string>Aiko Chat</string>
		<key>CFBundleExecutable</key>
		<string>$(EXECUTABLE_NAME)</string>
		<key>CFBundleIdentifier</key>
		<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
		<key>CFBundleInfoDictionaryVersion</key>
		<string>6.0</string>
		<key>CFBundleName</key>
		<string>aiko_chat_app</string>
		<key>CFBundlePackageType</key>
		<string>APPL</string>
		<key>CFBundleShortVersionString</key>
		<string>$(FLUTTER_BUILD_NAME)</string>
		<key>CFBundleSignature</key>
		<string>????</string>
		<key>CFBundleVersion</key>
		<string>$(FLUTTER_BUILD_NUMBER)</string>
		<key>LSRequiresIPhoneOS</key>
		<true/>
		<key>UIApplicationSceneManifest</key>
		<dict>
			<key>UIApplicationSupportsMultipleScenes</key>
			<false/>
			<key>UISceneConfigurations</key>
			<dict>
				<key>UIWindowSceneSessionRoleApplication</key>
				<array>
					<dict>
						<key>UISceneClassName</key>
						<string>UIWindowScene</string>
						<key>UISceneConfigurationName</key>
						<string>flutter</string>
						<key>UISceneDelegateClassName</key>
						<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
						<key>UISceneStoryboardFile</key>
						<string>Main</string>
					</dict>
				</array>
			</dict>
		</dict>
		<key>UIApplicationSupportsIndirectInputEvents</key>
		<true/>
		<key>UILaunchStoryboardName</key>
		<string>LaunchScreen</string>
		<key>UIMainStoryboardFile</key>
		<string>Main</string>
		<key>UISupportedInterfaceOrientations</key>
		<array>
			<string>UIInterfaceOrientationPortrait</string>
			<string>UIInterfaceOrientationLandscapeLeft</string>
			<string>UIInterfaceOrientationLandscapeRight</string>
		</array>
		<key>UISupportedInterfaceOrientations~ipad</key>
		<array>
			<string>UIInterfaceOrientationPortrait</string>
			<string>UIInterfaceOrientationPortraitUpsideDown</string>
			<string>UIInterfaceOrientationLandscapeLeft</string>
			<string>UIInterfaceOrientationLandscapeRight</string>
		</array>
		<key>UIStatusBarHidden</key>
		<false/>
		<!-- App uses only standard HTTPS/TLS, which is exempt from export
		     compliance. Declaring this avoids the per-build encryption
		     question at upload/submission time. -->
		<key>ITSAppUsesNonExemptEncryption</key>
		<false/>
		<!-- Firebase's own switch for "do not touch my AppDelegate", used here
		     because the alternative is finding out on a handset.

		     MEASURED, not assumed: `nm` on the built iOS binary shows
		     FIRMessagingRemoteNotificationsProxy and its swizzleSelector:
		     machinery linked in, hooking
		     application:didFailToRegisterForRemoteNotificationsWithError: and
		     its didRegister counterpart — the exact two callbacks this app now
		     takes its APNs device token from. The Firebase iOS SDK arrives via
		     the Dart plugins we need for ANDROID (see DevicePlatform, and
		     claude-tasks#3269 to remove the linkage entirely), and it is inert
		     only in the sense that `initializeApp` is never called on Apple —
		     which is a claim about initialisation, NOT about a swizzler
		     installed at load time.

		     A stolen callback would present as no token, no registration and no
		     error: the same silence as every other failure on this path. This
		     key states the relationship rather than leaving it to whichever SDK
		     ran first. -->
		<key>FirebaseAppDelegateProxyEnabled</key>
		<false/>
	</dict>
</plist>
