# Tesla (Grok) — DARK SEAT

Produced no verdict across two attempts (75KB bundle, then 60KB). Both returned a
short note claiming the bundle was truncated and announcing an intent to read the
offloaded prompt as a file — which the brief forbids ("do NOT run tools"). Verbatim
first attempt:

> The strike bundle was truncated. I’ll read the full prompt so the design, research,
> and charges are complete before writing the Tesla verdict.

Recorded so the empty seat reads as a COVERAGE GAP and never as a SOUND vote.
Tesla's bias is under-counted blast radius and future failure modes; that is the
lens this strike is thinnest on.
