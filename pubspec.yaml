name: aiko_chat_app
description: "Aiko Chat — mobile app (Flutter, Riverpod). Talks to the aiko-chat-island WSS+REST contract."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.0.3+11

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  riverpod_annotation: ^4.0.3
  go_router: ^17.3.0
  livekit_client: ^2.10.0
  web_socket_channel: ^3.0.3
  dio: ^5.9.2
  flutter_secure_storage: ^10.3.1
  drift: ^2.34.0
  sqlite3_flutter_libs: ^0.6.0+eol
  uuid: ^4.5.3
  connectivity_plus: ^7.1.1
  path_provider: ^2.1.6
  path: ^1.9.1
  shared_preferences: ^2.3.2
  # Passkeys via the federated implementation packages directly (NOT the
  # `passkeys` umbrella) — the umbrella declares a vestigial `ua_client_hints`
  # dep that lacks macOS Swift Package Manager support and forces a CocoaPods
  # fallback on the macOS target. These three pull no such dep, keeping macOS
  # pure-SPM. We call `PasskeysPlatform.instance` directly; the *_darwin/_android
  # packages self-register it via their `dartPluginClass`.
  passkeys_platform_interface: ^2.6.0
  passkeys_darwin: ^0.4.0
  passkeys_android: ^2.12.0
  cryptography: ^2.9.0
  device_info_plus: ^13.2.0
  package_info_plus: ^10.2.0
  share_plus: ^13.2.0
  google_fonts: ^8.2.1

# livekit_client ^2.10.0 (latest) pins device_info_plus ^12.3.0, but the rest of
# our tree is on 13.x (package_info_plus needs win32 ^6, which 12.x can't give).
# device_info_plus 13.x is API-compatible for the model/version fields both we
# (error_report.dart) and livekit read; the 12→13 bump was a min-SDK requirement.
# Drop this override once livekit_client supports device_info_plus 13.
dependency_overrides:
  device_info_plus: ^13.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# App icon generation. Regenerate after editing the source art with:
#   dart run flutter_launcher_icons
# Source art (SVG masters) lives in assets/branding/.
flutter_launcher_icons:
  image_path: "assets/branding/app_icon_1024.png"
  remove_alpha_ios: true
  android: true
  ios: true
  min_sdk_android: 21
  adaptive_icon_background: "#5E35B1"
  adaptive_icon_foreground: "assets/branding/app_icon_foreground.png"
  macos:
    generate: true
    image_path: "assets/branding/app_icon_1024.png"

# Native splash / launch screen. Regenerate after editing the source art with:
#   dart run flutter_native_splash:create
flutter_native_splash:
  color: "#5E35B1"
  image: "assets/branding/splash_logo.png"
  android_12:
    color: "#5E35B1"
    image: "assets/branding/splash_logo.png"
  ios: true
  android: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/legal/eula.md
    # SIL OFL 1.1 requires the licence travel WITH the font — including fonts
    # FETCHED at runtime, which google_fonts does NOT register itself. Wired
    # into LicenseRegistry at startup by lib/app/font_licences.dart, so these
    # appear in the standard licences page rather than sitting inert in the
    # bundle. OFL.txt covers the bundled IBM Plex Mono; licences/ carries one
    # file per fetchable family (each has its own copyright line).
    - assets/fonts/OFL.txt
    - assets/fonts/licences/

  # Maritime redesign type system (crucible docs/crucible/chatskin):
  # body/display text uses the platform-native UI font (no bundled family);
  # IBM Plex Mono is the "instrument voice" for ids, timestamps, and keys.
  fonts:
    - family: IBM Plex Mono
      fonts:
        - asset: assets/fonts/IBMPlexMono-Regular.ttf
        - asset: assets/fonts/IBMPlexMono-Medium.ttf
          weight: 500

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
